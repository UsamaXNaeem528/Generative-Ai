# class Student:
#     def __init__(self, name, marks):
#         self.name = name
#         self.marks = marks
        
#     def marks_avg(self):     
#         sum = 0
#         for value in self.marks: #--> in this marks is list so,we iterate this list for sum its all using loop
#             sum += value
#         print(f"Average Marks of {self.name} : {sum/3}")
        
# s1 = Student("Usama", [10,20,30])
# s1.marks_avg()        

# #* If in future, we have need to change the name Usama to anyother name, so we can directly change it like below.
# s1.name = 'Ironman'
# s1.marks = [40,50,60]
# s1.marks_avg()



