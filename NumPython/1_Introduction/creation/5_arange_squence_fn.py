# array with squence of numbers
#! it's arange() not arrange()
# np.arange(start, end, step) ==> start(starting number), end(when to end),
# step(jump size from current number)

import numpy as np
arr = np.arange(1,12,2)
print(arr)